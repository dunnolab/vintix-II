# Python environment for MetaDrive

## Building docker image for MetaDrive
Run the following command to build the image:

```shell
docker build --platform linux/amd64 -t metadrive-image:version1 .
```
