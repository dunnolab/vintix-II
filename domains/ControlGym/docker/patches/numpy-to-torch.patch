From 6d918e2d5b016c2fafaf1c99fed736092be67864 Mon Sep 17 00:00:00 2001
From: Alexander Serkov <32331578+setday@users.noreply.github.com>
Date: Fri, 29 Aug 2025 12:50:57 +0300
Subject: [PATCH] Numpy to Torch

---
 controlgym/controllers/lqg.py                 |  20 ++-
 controlgym/controllers/lqr.py                 |  22 +++-
 controlgym/envs/allen_cahn.py                 |  17 ++-
 controlgym/envs/burgers.py                    |  26 ++--
 controlgym/envs/cahn_hilliard.py              |  21 +--
 .../envs/convection_diffusion_reaction.py     |  40 +++---
 controlgym/envs/fisher.py                     |  17 +--
 controlgym/envs/ginzburg_landau.py            |  17 ++-
 controlgym/envs/korteweg_de_vries.py          |  15 ++-
 controlgym/envs/kuramoto_sivashinsky.py       |  19 +--
 controlgym/envs/pde.py                        | 120 ++++++++++--------
 controlgym/envs/schrodinger.py                |  86 +++++++------
 controlgym/envs/utils.py                      |   7 +-
 controlgym/envs/wave.py                       |  84 ++++++------
 14 files changed, 301 insertions(+), 210 deletions(-)

diff --git a/controlgym/controllers/lqg.py b/controlgym/controllers/lqg.py
index 0efb014..18dfd5e 100644
--- a/controlgym/controllers/lqg.py
+++ b/controlgym/controllers/lqg.py
@@ -1,6 +1,7 @@
 import numpy as np
 import logging
 from scipy.linalg import solve_discrete_are, inv, LinAlgError
+import torch
 
 
 def _lqr_gain(A: np.ndarray[float], B: np.ndarray[float], Q: np.ndarray[float], 
@@ -108,6 +109,10 @@ class LQG:
     def __init__(self, env):
         self.env = env
 
+        self.target_state = None
+        if hasattr(self.env, "target_state"):
+            self.target_state = env.target_state.detach().cpu().numpy()
+
         # check whether the problem is linear
         is_linear = self.env.category == "linear" or self.env.id in [
             "convection_diffusion_reaction", "wave", "schrodinger"]
@@ -124,6 +129,17 @@ class LQG:
             else np.zeros((self.env.n_state, self.env.n_action))
         )
 
+        if isinstance(A, torch.Tensor):
+            A = A.detach().cpu().numpy()
+        if isinstance(B2, torch.Tensor):
+            B2 = B2.detach().cpu().numpy()
+        if isinstance(Q, torch.Tensor):
+            Q = Q.detach().cpu().numpy()
+        if isinstance(R, torch.Tensor):
+            R = R.detach().cpu().numpy()
+        if isinstance(S, torch.Tensor):
+            S = S.detach().cpu().numpy()
+
         # PDE environments
         if hasattr(self.env, "sensor_noise_cov"):
             assert (
@@ -164,8 +180,8 @@ class LQG:
         Returns:
             action: ndarray[float], control input.
         """
-        if hasattr(self.env, "target_state"):
-            return self.gain_lqr @ est_state + self.gain_lqt @ self.env.target_state
+        if self.target_state is not None:
+            return self.gain_lqr @ est_state + self.gain_lqt @ self.target_state
         else:
             return self.gain_lqr @ est_state
 
diff --git a/controlgym/controllers/lqr.py b/controlgym/controllers/lqr.py
index e91f5d5..c6ed964 100644
--- a/controlgym/controllers/lqr.py
+++ b/controlgym/controllers/lqr.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from numpy.linalg import inv
 import logging
 from scipy.linalg import solve_discrete_are, LinAlgError
@@ -78,6 +79,10 @@ class LQR:
     def __init__(self, env):
         self.env = env
 
+        self.target_state = None
+        if hasattr(self.env, "target_state"):
+            self.target_state = env.target_state.detach().cpu().numpy()
+
         # check whether the problem is linear
         is_linear = self.env.category == "linear" or self.env.id in [
             "convection_diffusion_reaction",
@@ -93,6 +98,17 @@ class LQR:
         R = self.env.R if hasattr(self.env, "R") else np.identity(self.env.n_action)
         S = self.env.S if hasattr(self.env, "S") else np.zeros((self.env.n_state, self.env.n_action))
 
+        if isinstance(A, torch.Tensor):
+            A = A.detach().cpu().numpy()
+        if isinstance(B2, torch.Tensor):
+            B2 = B2.detach().cpu().numpy()
+        if isinstance(Q, torch.Tensor):
+            Q = Q.detach().cpu().numpy()
+        if isinstance(R, torch.Tensor):
+            R = R.detach().cpu().numpy()
+        if isinstance(S, torch.Tensor):
+            S = S.detach().cpu().numpy()
+
         # compute the LQR gain
         self.gain_lqr, self.gain_lqt = _lqr_gain(A, B2, Q, R, S)
 
@@ -105,9 +121,9 @@ class LQR:
         Returns:
             action: ndarray[float], control input.
         """
-        # PDE settings where the objective is to trak a target state
-        if hasattr(self.env, "target_state"):
-            return self.gain_lqr @ state + self.gain_lqt @ self.env.target_state
+        # PDE settings where the objective is to track a target state
+        if self.target_state is not None:
+            return self.gain_lqr @ state + self.gain_lqt @ self.target_state
         else:
             return self.gain_lqr @ state
 
diff --git a/controlgym/envs/allen_cahn.py b/controlgym/envs/allen_cahn.py
index 220661e..2f56af0 100644
--- a/controlgym/envs/allen_cahn.py
+++ b/controlgym/envs/allen_cahn.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -64,6 +65,7 @@ class AllenCahnEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -84,7 +86,8 @@ class AllenCahnEnv(PDE):
             action_limit=action_limit,
             observation_limit=observation_limit,
             reward_limit=reward_limit,
-            seed=seed, 
+            seed=seed,
+            device=device,
         )
 
         # physical parameters
@@ -99,12 +102,12 @@ class AllenCahnEnv(PDE):
     def select_init_state(self, init_offset=None):
         """Function to select the initial state of the PDE."""
         if init_offset is None:
-            random_offset = self.rng.uniform(-0.5 * self.init_offset_width, 0.5 * self.init_offset_width)
+            random_offset = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_offset_width
             init_offset = self.init_offset_mean + random_offset
         init_state = init_offset + (
             self.domain_coordinates - 0.5 * self.domain_length
-        ) ** 2 * np.cos(
-            (2 * np.pi * (self.domain_coordinates - 0.5 * self.domain_length))
+        ) ** 2 * torch.cos(
+            (2 * torch.pi * (self.domain_coordinates - 0.5 * self.domain_length))
             / self.domain_length
         )
         return init_state
@@ -137,12 +140,12 @@ class AllenCahnEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
-            right_hand_side = -self.potential_constant * (1 / aa_factor) * np.fft.rfft(
+            right_hand_side = -self.potential_constant * (1 / aa_factor) * torch.fft.rfft(
                 aa_state ** 3
-            )[0 : int(self.n_state / 2) + 1] + (np.fft.rfft(self.control_sup, axis=0) @ action)
+            )[0 : int(self.n_state / 2) + 1] + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
         return fourier_nonlinear_op
 
diff --git a/controlgym/envs/burgers.py b/controlgym/envs/burgers.py
index 6b0a8dd..215215a 100644
--- a/controlgym/envs/burgers.py
+++ b/controlgym/envs/burgers.py
@@ -1,3 +1,4 @@
+import torch
 import numpy as np
 from controlgym.envs import PDE
 
@@ -67,6 +68,7 @@ class BurgersEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -88,6 +90,7 @@ class BurgersEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # physical parameter
@@ -103,12 +106,12 @@ class BurgersEnv(PDE):
     def select_init_state(self, init_amplitude=None, init_spread=None):
         """Function to select the initial state of the PDE"""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(-0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width)
-            init_amplitude = self.init_amplitude_mean + random_amplitude
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
+            init_amplitude = self.init_amplitude_mean + random_amplitude.item()
         if init_spread is None:
-            random_spread = self.rng.uniform(-0.5 * self.init_spread_width, 0.5 * self.init_spread_width)
-            init_spread = self.init_spread_mean + random_spread
-        init_state = init_amplitude * np.cosh(
+            random_spread = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_spread_width
+            init_spread = self.init_spread_mean + random_spread.item()
+        init_state = init_amplitude * torch.cosh(
             1 / init_spread * (self.domain_coordinates - 0.5 * self.domain_length)
         ) ** (-1)
         return init_state
@@ -139,16 +142,15 @@ class BurgersEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
-                state_fourier, n=int(self.n_state * aa_factor)
-            )
-            right_hand_side = (
+            n_aa = int(self.n_state * aa_factor)
+            aa_state = aa_factor * torch.fft.irfft(state_fourier, n=n_aa)
+            rhs = (
                 -0.5j
                 * self.domain_wavenumbers
                 * (1 / aa_factor)
-                * np.fft.rfft(aa_state**2)[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
-            return right_hand_side
+                * torch.fft.rfft(aa_state ** 2)[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
+            return rhs
 
         return fourier_nonlinear_op
 
diff --git a/controlgym/envs/cahn_hilliard.py b/controlgym/envs/cahn_hilliard.py
index 82f8d3e..2d858c6 100644
--- a/controlgym/envs/cahn_hilliard.py
+++ b/controlgym/envs/cahn_hilliard.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -66,6 +67,7 @@ class CahnHilliardEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -87,6 +89,7 @@ class CahnHilliardEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # physical parameters
@@ -101,14 +104,12 @@ class CahnHilliardEnv(PDE):
     def select_init_state(self, init_amplitude=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
-        init_state = init_amplitude * np.cos(
-            np.pi * (self.domain_coordinates - 0.5 * self.domain_length)
-        ) - np.exp(
-            -4 * (np.pi * (self.domain_coordinates - 0.5 * self.domain_length)) ** 2
+        init_state = init_amplitude * torch.cos(
+            torch.pi * (self.domain_coordinates - 0.5 * self.domain_length)
+        ) - torch.exp(
+            -4 * (torch.pi * (self.domain_coordinates - 0.5 * self.domain_length)) ** 2
         )
         return init_state
 
@@ -141,15 +142,15 @@ class CahnHilliardEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
             right_hand_side = (
                 -self.diffusivity_constant
                 * self.domain_wavenumbers**2
                 * (1 / aa_factor)
-                * np.fft.rfft(aa_state**3)[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
+                * torch.fft.rfft(aa_state**3)[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
 
         return fourier_nonlinear_op
diff --git a/controlgym/envs/convection_diffusion_reaction.py b/controlgym/envs/convection_diffusion_reaction.py
index 36c57c9..0d2e8bd 100644
--- a/controlgym/envs/convection_diffusion_reaction.py
+++ b/controlgym/envs/convection_diffusion_reaction.py
@@ -1,4 +1,6 @@
 import numpy as np
+import torch
+import math
 from controlgym.envs import PDE
 from controlgym.envs.utils import ft_matrix, ift_matrix
 
@@ -74,6 +76,7 @@ class ConvectionDiffusionReactionEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -95,6 +98,7 @@ class ConvectionDiffusionReactionEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # physical parameters
@@ -116,16 +120,12 @@ class ConvectionDiffusionReactionEnv(PDE):
     def select_init_state(self, init_amplitude=None, init_spread=None):
         """Function to select the initial state of the PDE"""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         if init_spread is None:
-            random_spread = self.rng.uniform(
-                -0.5 * self.init_spread_width, 0.5 * self.init_spread_width
-            )
+            random_spread = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_spread_width
             init_spread = self.init_spread_mean + random_spread
-        init_state = init_amplitude * np.cosh(
+        init_state = init_amplitude * torch.cosh(
             1 / init_spread * (self.domain_coordinates - 0.5 * self.domain_length)
         ) ** (-1)
         return init_state
@@ -159,23 +159,30 @@ class ConvectionDiffusionReactionEnv(PDE):
         ), "Input control has wrong dimension, the correct dimension is: " + str(
             (self.n_action,)
         )
+
+        action_torch = torch.tensor(action, device=self.device, dtype=self.dtype)
+
         # generate the process noise, which is a Gaussian random vector with dimension n_state
-        disturbance = self.rng.multivariate_normal(
-            np.zeros(self.n_state),
-            self.process_noise_cov * np.identity(self.n_state),
+        disturbance = torch.normal(
+            mean=0.0,
+            std=self.process_noise_cov,
+            size=(self.n_state,),
+            generator=self.rng,
+            device=self.device,
+            dtype=self.dtype
         )
 
         # compute the observation
         observation = self._get_obs()
 
         # compute the next state
-        next_state = self.A @ self.state + self.B2 @ action + disturbance
+        next_state = self.A @ self.state + self.B2 @ action_torch + disturbance
 
         # compute the reward, which happens before updating the environment state
         # because the reward (might) depends on both the current state and the next state.
         # * In the default reward function, the dependence on the current state is
         # through the self.state attribute, which will not be updated until the next line.
-        reward = self.get_reward(action, observation, disturbance, next_state)
+        reward = self.get_reward(action_torch, observation, disturbance, next_state)
 
         # update the environment
         self.state = next_state
@@ -186,7 +193,8 @@ class ConvectionDiffusionReactionEnv(PDE):
         truncated = (
             False if self.reward_range[0] <= reward <= self.reward_range[1] else True
         )
-        info = {"state": self.state}
+        info = {"state": self.state.detach().cpu().numpy()}
+        observation = observation.detach().cpu().numpy()
 
         return observation, reward, terminated, truncated, info
 
@@ -202,9 +210,9 @@ class ConvectionDiffusionReactionEnv(PDE):
         DFT = ft_matrix(self.n_state)  # discrete fourier transform matrix
         IDFT = ift_matrix(self.n_state)  # inverse fourier transform matrix
         domain_wavenumber_complex = (
-            np.fft.fftfreq(self.n_state, self.domain_length / self.n_state) * 2 * np.pi
+            torch.fft.fftfreq(self.n_state, self.domain_length / self.n_state) * 2 * torch.pi
         )  # wavenumbers
-        eigen = np.exp(
+        eigen = torch.exp(
                     (
                         -self.convective_velocity * 1j * domain_wavenumber_complex
                         - self.diffusivity_constant * domain_wavenumber_complex**2
@@ -212,7 +220,7 @@ class ConvectionDiffusionReactionEnv(PDE):
                     )
                     * self.sample_time
                 )
-        A = np.real(IDFT @ np.diag(eigen) @ DFT)
+        A = torch.real(IDFT @ torch.diag(eigen).to(dtype=IDFT.dtype) @ DFT)
         return A, eigen
 
     def get_params_asdict(self):
diff --git a/controlgym/envs/fisher.py b/controlgym/envs/fisher.py
index 962bb8e..7810516 100644
--- a/controlgym/envs/fisher.py
+++ b/controlgym/envs/fisher.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -66,6 +67,7 @@ class FisherEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -87,6 +89,7 @@ class FisherEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # physical parameters
@@ -101,12 +104,10 @@ class FisherEnv(PDE):
     def select_init_state(self, init_amplitude=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
-        init_state = init_amplitude * np.sin(
-            4 * np.pi * self.domain_coordinates / self.domain_length
+        init_state = init_amplitude * torch.sin(
+            4 * torch.pi * self.domain_coordinates / self.domain_length
         )
         return init_state
 
@@ -136,14 +137,14 @@ class FisherEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
             right_hand_side = (
                 self.reaction_constant
                 * (1 / aa_factor)
-                * np.fft.rfft(aa_state * (1 - aa_state))[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
+                * torch.fft.rfft(aa_state * (1 - aa_state))[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
 
         return fourier_nonlinear_op
diff --git a/controlgym/envs/ginzburg_landau.py b/controlgym/envs/ginzburg_landau.py
index fdf098a..9a44c43 100644
--- a/controlgym/envs/ginzburg_landau.py
+++ b/controlgym/envs/ginzburg_landau.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -65,6 +66,7 @@ class GinzburgLandauEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -86,6 +88,7 @@ class GinzburgLandauEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # initial state parameters
@@ -98,14 +101,14 @@ class GinzburgLandauEnv(PDE):
     def select_init_state(self, init_amplitude=None, init_spread=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(-0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width)
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         if init_spread is None:
-            random_spread = self.rng.uniform(-0.5 * self.init_spread_width, 0.5 * self.init_spread_width)
+            random_spread = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_spread_width
             init_spread = self.init_spread_mean + random_spread
         init_state = init_amplitude * (
-            np.tanh(self.domain_coordinates - self.domain_length / 2 + init_spread / 2)
-            - np.tanh(
+            torch.tanh(self.domain_coordinates - self.domain_length / 2 + init_spread / 2)
+            - torch.tanh(
                 self.domain_coordinates - self.domain_length / 2 - init_spread / 2
             )
         )
@@ -137,13 +140,13 @@ class GinzburgLandauEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
             right_hand_side = (
                 -(1 / aa_factor)
-                * np.fft.rfft(aa_state**3)[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
+                * torch.fft.rfft(aa_state**3)[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
 
         return fourier_nonlinear_op
diff --git a/controlgym/envs/korteweg_de_vries.py b/controlgym/envs/korteweg_de_vries.py
index 7f86ee6..49ba83a 100644
--- a/controlgym/envs/korteweg_de_vries.py
+++ b/controlgym/envs/korteweg_de_vries.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -61,6 +62,7 @@ class KortewegDeVriesEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -82,6 +84,7 @@ class KortewegDeVriesEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # initial state parameters
@@ -92,14 +95,14 @@ class KortewegDeVriesEnv(PDE):
     def select_init_state(self, init_amplitude=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(-0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width)
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         init_state = (
             -0.5
             * init_amplitude
-            * np.cosh(
+            * torch.cosh(
                 0.5
-                * np.sqrt(init_amplitude)
+                * torch.sqrt(init_amplitude)
                 * (self.domain_coordinates - 0.5 * self.domain_length)
             )
             ** (-2)
@@ -132,15 +135,15 @@ class KortewegDeVriesEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
             right_hand_side = (
                 3j
                 * self.domain_wavenumbers
                 * (1 / aa_factor)
-                * np.fft.rfft(aa_state**2)[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
+                * torch.fft.rfft(aa_state**2)[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
 
         return fourier_nonlinear_op
diff --git a/controlgym/envs/kuramoto_sivashinsky.py b/controlgym/envs/kuramoto_sivashinsky.py
index b3bad36..3af35e0 100644
--- a/controlgym/envs/kuramoto_sivashinsky.py
+++ b/controlgym/envs/kuramoto_sivashinsky.py
@@ -1,4 +1,5 @@
 import numpy as np
+import torch
 from controlgym.envs import PDE
 
 
@@ -43,7 +44,7 @@ class KuramotoSivashinskyEnv(PDE):
     def __init__(
         self,
         n_steps: int = 500,
-        domain_length: float = 32 * np.pi,
+        domain_length: float = 32 * torch.pi,
         integration_time: float = 0.005,
         sample_time: float = 0.5,
         process_noise_cov: float = 0.0,
@@ -61,6 +62,7 @@ class KuramotoSivashinskyEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         PDE.__init__(
             self,
@@ -82,6 +84,7 @@ class KuramotoSivashinskyEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # initial state parameters
@@ -92,14 +95,12 @@ class KuramotoSivashinskyEnv(PDE):
     def select_init_state(self, init_amplitude=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         init_state = (
             init_amplitude
-            * np.cos(2 * np.pi * self.domain_coordinates / self.domain_length)
-            * np.sin(12 * np.pi * self.domain_coordinates / self.domain_length)
+            * torch.cos(2 * torch.pi * self.domain_coordinates / self.domain_length)
+            * torch.sin(12 * torch.pi * self.domain_coordinates / self.domain_length)
         )
         return init_state
 
@@ -129,15 +130,15 @@ class KuramotoSivashinskyEnv(PDE):
             # aa_state is the anti-aliased state; meaning the state evaluated in
             # physical space on a grid with 3/2 times more points
             aa_factor = 3 / 2
-            aa_state = aa_factor * np.fft.irfft(
+            aa_state = aa_factor * torch.fft.irfft(
                 state_fourier, n=int(self.n_state * aa_factor)
             )
             right_hand_side = (
                 -0.5j
                 * self.domain_wavenumbers
                 * (1 / aa_factor)
-                * np.fft.rfft(aa_state**2)[0 : int(self.n_state / 2) + 1]
-            ) + (np.fft.rfft(self.control_sup, axis=0) @ action)
+                * torch.fft.rfft(aa_state**2)[0 : int(self.n_state / 2) + 1]
+            ) + (torch.fft.rfft(self.control_sup, dim=0) @ action.to(dtype=torch.complex128))
             return right_hand_side
 
         return fourier_nonlinear_op
diff --git a/controlgym/envs/pde.py b/controlgym/envs/pde.py
index 90602c2..f34a716 100644
--- a/controlgym/envs/pde.py
+++ b/controlgym/envs/pde.py
@@ -1,6 +1,7 @@
 import numpy as np
 import gymnasium
 import math
+import torch
 
 
 class PDE(gymnasium.Env):
@@ -62,14 +63,18 @@ class PDE(gymnasium.Env):
         observation_limit: float,
         reward_limit: float,
         seed: int,
+        device: str = 'cpu',
+        dtype: torch.dtype = torch.float64
     ):
         self.id = id
         self.category = "pde"
-
+        self.device = device
         self.n_steps = n_steps
         self.integration_time = integration_time
         self.sample_time = sample_time
 
+        self.dtype = dtype
+
         # check whether sample_time is a integer multiple of integration_time
         if integration_time is not None:
             assert (
@@ -80,13 +85,15 @@ class PDE(gymnasium.Env):
         self.sensor_noise_cov = sensor_noise_cov
 
         self.seed = seed
-        self.rng = np.random.default_rng(seed=self.seed)
+        self.rng = torch.Generator(device=self.device)
+        if self.seed is not None:
+            self.rng.manual_seed(self.seed)
 
         self.n_state = n_state
         # check whether the n_state is even
         assert self.n_state % 2 == 0, "n_state must be even."
 
-        self.target_state = np.zeros(self.n_state) if target_state is None else target_state
+        self.target_state = torch.zeros(self.n_state if target_state is None else target_state, device=self.device, dtype=self.dtype)
 
         self.n_observation = n_observation
         self.n_action = n_action
@@ -99,11 +106,11 @@ class PDE(gymnasium.Env):
 
         # set up the grid parameters
         self.domain_length = domain_length  # domain length
-        self.domain_coordinates = np.linspace(
-            0, self.domain_length - (self.domain_length / self.n_state), self.n_state
+        self.domain_coordinates = torch.linspace(
+            0, self.domain_length - (self.domain_length / self.n_state), self.n_state, device=self.device
         )
         self.domain_wavenumbers = (
-            np.fft.rfftfreq(self.n_state, self.domain_length / self.n_state) * 2 * np.pi
+            torch.fft.rfftfreq(self.n_state, self.domain_length / self.n_state, device=self.device) * 2 * math.pi
         )
 
         # compute control support and observation matrix
@@ -111,15 +118,13 @@ class PDE(gymnasium.Env):
         self.C = self._compute_C()
 
         # set up the weighting matrices
-        self.Q = Q_weight * np.identity(self.n_state)
-        self.R = R_weight * np.identity(self.n_action)
+        self.Q = Q_weight * torch.eye(self.n_state, device=self.device, dtype=self.dtype)
+        self.R = R_weight * torch.eye(self.n_action, device=self.device, dtype=self.dtype)
 
         # set up the gym.Env attributes
-        self.action_limit = np.inf if action_limit is None else action_limit
-        self.observation_limit = (
-            np.inf if observation_limit is None else observation_limit
-        )
-        self.reward_limit = np.inf if reward_limit is None else reward_limit
+        self.action_limit = float('inf') if action_limit is None else action_limit
+        self.observation_limit = float('inf') if observation_limit is None else observation_limit
+        self.reward_limit = float('inf') if reward_limit is None else reward_limit
 
         self.action_space = gymnasium.spaces.Box(
             low=-self.action_limit,
@@ -138,6 +143,8 @@ class PDE(gymnasium.Env):
         self.step_count = 0
         self.state_traj = None
 
+        self.state = torch.zeros(self.n_state, device=self.device, dtype=self.dtype)
+
     def step(self, action: np.ndarray[float]):
         """Run one timestep of the environment's dynamics using the agent actions.
 
@@ -166,15 +173,21 @@ class PDE(gymnasium.Env):
         ), "Input control has wrong dimension, the correct dimension is: " + str(
             (self.n_action,)
         )
+        action_torch = torch.tensor(action, device=self.device, dtype=self.dtype)
 
         # sample the process noise, which is a Gaussian random vector with dimension n_state
-        disturbance = self.rng.multivariate_normal(
-            np.zeros(self.n_state),
-            self.process_noise_cov * np.identity(self.n_state),
+        disturbance = torch.normal(
+            mean=0.0,
+            std=self.process_noise_cov,
+            size=(self.n_state,),
+            generator=self.rng,
+            device=self.device,
+            dtype=self.dtype
         )
 
         # generate the observation
         observation = self._get_obs()
+        observation = torch.clamp(observation, -self.observation_limit, self.observation_limit)
 
         # evolve the PDE dynamics using numerical integration
         # for Convection-Diffusion-Reaction, Wave, and Schodinger,
@@ -191,34 +204,34 @@ class PDE(gymnasium.Env):
         num_integration_steps = int(self.sample_time // self.integration_time)
 
         # precompute various ETDRK4 scalar quantities
-        E = np.exp(self.integration_time * fourier_linear_op)
-        E2 = np.exp(self.integration_time * fourier_linear_op / 2)
+        E = torch.exp(self.integration_time * fourier_linear_op)
+        E2 = torch.exp(self.integration_time * fourier_linear_op / 2)
         M = 32
-        r = np.exp(1j * np.pi * (np.arange(1, M + 1) - 0.5) / M)
-        LR = self.integration_time * fourier_linear_op[:, np.newaxis] + r[np.newaxis, :]
-        Q = self.integration_time * np.real(np.mean((np.exp(LR / 2) - 1) / LR, axis=1))
-        f1 = self.integration_time * np.real(
-            np.mean((-4 - LR + np.exp(LR) * (4 - 3 * LR + LR**2)) / LR**3, axis=1)
+        r = torch.exp(1j * math.pi * (torch.arange(1, M + 1, device=self.device) - 0.5) / M)
+        LR = self.integration_time * fourier_linear_op[:, None] + r[None, :]
+        Q = self.integration_time * torch.real(torch.mean((torch.exp(LR / 2) - 1) / LR, dim=1))
+        f1 = self.integration_time * torch.real(
+            torch.mean((-4 - LR + torch.exp(LR) * (4 - 3 * LR + LR**2)) / LR**3, dim=1)
         )
-        f2 = self.integration_time * np.real(
-            np.mean((2 + LR + np.exp(LR) * (-2 + LR)) / LR**3, axis=1)
+        f2 = self.integration_time * torch.real(
+            torch.mean((2 + LR + torch.exp(LR) * (-2 + LR)) / LR**3, dim=1)
         )
-        f3 = self.integration_time * np.real(
-            np.mean((-4 - 3 * LR - LR**2 + np.exp(LR) * (4 - LR)) / LR**3, axis=1)
+        f3 = self.integration_time * torch.real(
+            torch.mean((-4 - 3 * LR - LR**2 + torch.exp(LR) * (4 - LR)) / LR**3, dim=1)
         )
 
         # time-stepping loop
-        state_fourier = np.fft.rfft(self.state)
+        state_fourier = torch.fft.rfft(self.state)
         for _ in range(num_integration_steps):
             # catch RuntimeWarning: overflow
             try:
-                fourier_nonlinear_RHS = fourier_nonlinear_op(state_fourier, action)
+                fourier_nonlinear_RHS = fourier_nonlinear_op(state_fourier, action_torch)
                 a = E2 * state_fourier + Q * fourier_nonlinear_RHS
-                Na = fourier_nonlinear_op(a, action)
+                Na = fourier_nonlinear_op(a, action_torch)
                 b = E2 * state_fourier + Q * Na
-                Nb = fourier_nonlinear_op(b, action)
+                Nb = fourier_nonlinear_op(b, action_torch)
                 c = E2 * a + Q * (2 * Nb - fourier_nonlinear_RHS)
-                Nc = fourier_nonlinear_op(c, action)
+                Nc = fourier_nonlinear_op(c, action_torch)
                 state_fourier = (
                     E * state_fourier
                     + fourier_nonlinear_RHS * f1
@@ -232,13 +245,13 @@ class PDE(gymnasium.Env):
                 break
 
         # set the new system state
-        next_state = np.fft.irfft(state_fourier) + disturbance
+        next_state = torch.fft.irfft(state_fourier) + disturbance
 
         # compute the reward, which happens before updating the environment state
         # because the reward (might) depends on both the current state and the next state.
         # * In the default reward function, the dependence on the current state is
         # through the self.state attribute, which will not be updated until the next line.
-        reward = self.get_reward(action, observation, disturbance, next_state)
+        reward = self.get_reward(action_torch, observation, disturbance, next_state)
 
         # update the environment
         self.state = next_state
@@ -249,12 +262,13 @@ class PDE(gymnasium.Env):
         truncated = (
             False if self.reward_range[0] <= reward <= self.reward_range[1] else True
         )
-        info = {"state": self.state}
+        info = {"state": self.state.detach().cpu().numpy()}
+        observation = observation.detach().cpu().numpy()
 
         # return the observation and stage cost
         return observation, reward, terminated, truncated, info
 
-    def reset(self, seed: int = None, state: np.ndarray[float] = None):
+    def reset(self, seed: int = None, state: torch.Tensor = None, **kwargs):
         """Resets the environment to an initial internal state, returning an initial observation and info.
 
         This method generates a new starting state with some randomness to ensure that the agent explores the
@@ -276,7 +290,7 @@ class PDE(gymnasium.Env):
         """
         # reset the random number generator if there is a new seed provided
         if seed is not None:
-            self.rng = np.random.default_rng(seed=seed)
+            self.rng.manual_seed(seed)
 
         # randomly generate a state (see child classes for individual implementations)
         if state is None:
@@ -287,11 +301,13 @@ class PDE(gymnasium.Env):
             ), "Input state has wrong dimension, the correct dimension is: " + str(
                 (self.n_state,)
             )
-        self.state = state
+        self.state = state.to(self.device, dtype=self.dtype)
 
         # generate the observation
         observation = self._get_obs()
-        info = {"state": self.state}
+        observation = torch.clamp(observation, -self.observation_limit, self.observation_limit)
+        observation = observation.detach().cpu().numpy()
+        info = {"state": self.state.detach().cpu().numpy()}
         self.step_count = 0
 
         return observation, info
@@ -311,7 +327,7 @@ class PDE(gymnasium.Env):
         ), "Input target state has wrong dimension, the correct dimension is: " + str(
             (self.n_state,)
         )
-        self.target_state = state
+        self.target_state = torch.tensor(state, device=self.device, dtype=self.dtype)
 
     def _get_obs(self):
         """private function to generate the observation for pdes
@@ -324,15 +340,19 @@ class PDE(gymnasium.Env):
                 where C is the observation matrix and noise is a Gaussian random vector with zero
                 mean and covariance matrix being self.sensor_noise_cov * I
         """
-        noise = self.rng.multivariate_normal(
-            np.zeros(self.n_observation),
-            self.sensor_noise_cov * np.identity(self.n_observation),
+        noise = torch.normal(
+            mean=0.0,
+            std=self.sensor_noise_cov,
+            size=(self.n_observation,),
+            generator=self.rng,
+            device=self.device,
+            dtype=self.dtype
         )
         observation = self.C @ self.state + noise
         return observation
 
-    def get_reward(self, action: np.ndarray[float], observation: np.ndarray[float], disturbance: np.ndarray[float], 
-                   next_state: np.ndarray[float]):
+    def get_reward(self, action: torch.Tensor, observation: torch.Tensor, disturbance: torch.Tensor,
+                   next_state: torch.Tensor):
         """ function to generate the reward for the current time step
 
         Args:
@@ -357,8 +377,8 @@ class PDE(gymnasium.Env):
             env.get_reward = custom_get_reward.__get__(env)
         ```
         """
-        reward = - float((self.state - self.target_state).T @ self.Q @ (self.state - self.target_state) 
-                         + action.T @ self.R @ action)
+        state_diff = self.state - self.target_state
+        reward = - float(state_diff @ self.Q @ state_diff + action @ self.R @ action)
         return reward
 
     def _compute_fourier_linear_op(self):
@@ -384,7 +404,7 @@ class PDE(gymnasium.Env):
                 which is 1. The nonzero entry is located at the position corresponding to the sensor location.
                 Each sensors are evenly distributed in the domain.
         """
-        C = np.zeros((self.n_observation, self.n_state))
+        C = torch.zeros((self.n_observation, self.n_state), device=self.device, dtype=self.dtype)
         # loop over all sensors and assign entries of C
         for idx in range(self.n_observation):
             i_x = self.n_state // self.n_observation * idx
@@ -405,7 +425,7 @@ class PDE(gymnasium.Env):
             control input can affect. It is enforced that control_sup_width * n_actions <= 1.
             Each control input is assumed to be evenly distributed in the domain.
         """
-        control_sup = np.zeros((self.n_state, self.n_action))
+        control_sup = torch.zeros((self.n_state, self.n_action), device=self.device, dtype=self.dtype)
         domain_width = self.control_sup_width * self.n_state
         domain_spacing = int(self.n_state / self.n_action)
         for u_i in range(self.n_action):
@@ -445,6 +465,6 @@ class PDE(gymnasium.Env):
             env_params["seed"] = self.seed
 
         if self.target_state is not None:
-            env_params["target_state"] = self.target_state
+            env_params["target_state"] = self.target_state.cpu().numpy()
 
         return env_params
diff --git a/controlgym/envs/schrodinger.py b/controlgym/envs/schrodinger.py
index f6fbf47..12f1f98 100644
--- a/controlgym/envs/schrodinger.py
+++ b/controlgym/envs/schrodinger.py
@@ -1,5 +1,6 @@
 import numpy as np
-import scipy as sp
+import math
+import torch
 from controlgym.envs import PDE
 from controlgym.envs.utils import ft_matrix, ift_matrix
 
@@ -78,6 +79,7 @@ class SchrodingerEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = None,
         seed: int = None,
+        device: str = 'cpu',
     ):
         self.n_state_half = int(n_state / 2)
         PDE.__init__(
@@ -100,14 +102,16 @@ class SchrodingerEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
-        # set up the grid parameters: since the state x comprises both the reak and
+        # set up the grid parameters: since the state x comprises both the real and
         # imaginary parts of u, the number of grid points is half the dimension of x
-        self.domain_coordinates = np.linspace(
+        self.domain_coordinates = torch.linspace(
             0,
             self.domain_length - (self.domain_length / self.n_state_half),
             self.n_state_half,
+            device=self.device
         )
 
         # set up the physical parameters
@@ -129,20 +133,16 @@ class SchrodingerEnv(PDE):
     def select_init_state(self, init_amplitude=None, init_spread=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         if init_spread is None:
-            random_spread = self.rng.uniform(
-                -0.5 * self.init_spread_width, 0.5 * self.init_spread_width
-            )
+            random_spread = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_spread_width
             init_spread = self.init_spread_mean + random_spread
-        init_u = init_amplitude * np.cosh(
+        init_u = init_amplitude * torch.cosh(
             1 / init_spread * (self.domain_coordinates - 1 * self.domain_length / 2)
         ) ** (-1)
-        init_v = np.zeros(self.n_state_half)
-        init_state = np.concatenate((init_u, init_v))
+        init_v = torch.zeros(self.n_state_half, device=self.device)
+        init_state = torch.cat((init_u, init_v))
         return init_state
 
     def step(self, action: np.ndarray[float]):
@@ -174,23 +174,30 @@ class SchrodingerEnv(PDE):
         ), "Input control has wrong dimension, the correct dimension is: " + str(
             (self.n_action,)
         )
+
+        action_torch = torch.tensor(action, device=self.device, dtype=self.dtype)
+
         # generate the process noise, which is a Gaussian random vector with dimension n_state
-        disturbance = self.rng.multivariate_normal(
-            np.zeros(self.n_state),
-            self.process_noise_cov * np.identity(self.n_state),
+        disturbance = torch.normal(
+            mean=0.0,
+            std=self.process_noise_cov,
+            size=(self.n_state,),
+            generator=self.rng,
+            device=self.device,
+            dtype=self.dtype
         )
 
         # compute the observation
         observation = self._get_obs()
 
         # compute the next state
-        next_state = self.A @ self.state + self.B2 @ action + disturbance
+        next_state = self.A @ self.state + self.B2 @ action_torch + disturbance
 
         # compute the reward, which happens before updating the environment state
         # because the reward (might) depends on both the current state and the next state.
         # * In the default reward function, the dependence on the current state is
         # through the self.state attribute, which will not be updated until the next line.
-        reward = self.get_reward(action, observation, disturbance, next_state)
+        reward = self.get_reward(action_torch, observation, disturbance, next_state)
 
         # update the environment
         self.state = next_state
@@ -201,7 +208,8 @@ class SchrodingerEnv(PDE):
         truncated = (
             False if self.reward_range[0] <= reward <= self.reward_range[1] else True
         )
-        info = {"state": self.state}
+        info = {"state": self.state.detach().cpu().numpy()}
+        observation = observation.detach().cpu().numpy()
 
         # return the observation and stage cost
         return observation, reward, terminated, truncated, info
@@ -213,64 +221,64 @@ class SchrodingerEnv(PDE):
             None.
 
         Returns:
-            A (`ndarray` with shape `(n_state, n_state)).
+            A (`Tensor` with shape `(n_state, n_state)).
         """
-        DFT = np.vstack(
+        DFT = torch.vstack(
             (
-                np.hstack(
+                torch.hstack(
                     (
                         ft_matrix(self.n_state_half),
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                     )
                 ),
-                np.hstack(
+                torch.hstack(
                     (
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                         ft_matrix(self.n_state_half),
                     )
                 ),
             )
         )
-        IDFT = np.vstack(
+        IDFT = torch.vstack(
             (
-                np.hstack(
+                torch.hstack(
                     (
                         ift_matrix(self.n_state_half),
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                     )
                 ),
-                np.hstack(
+                torch.hstack(
                     (
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                         ift_matrix(self.n_state_half),
                     )
                 ),
             )
         )
         domain_wavenumbers_complex = (
-            np.fft.fftfreq(self.n_state_half, self.domain_length / self.n_state_half)
+            torch.fft.fftfreq(self.n_state_half, self.domain_length / self.n_state_half)
             * 2
-            * np.pi
+            * torch.pi
         )  # wavenumbers
-        L12 = np.diag(
+        L12 = torch.diag(
             self.planck_constant
             / (2 * self.particle_mass)
             * domain_wavenumbers_complex**2
             + self.potential / self.planck_constant
         )
-        L21 = np.diag(
+        L21 = torch.diag(
             -self.planck_constant
             / (2 * self.particle_mass)
             * domain_wavenumbers_complex**2
             - self.potential / self.planck_constant
         )
-        L = np.vstack(
+        L = torch.vstack(
             (
-                np.hstack((np.zeros((self.n_state_half, self.n_state_half)), L12)),
-                np.hstack((L21, np.zeros((self.n_state_half, self.n_state_half)))),
+                torch.hstack((torch.zeros((self.n_state_half, self.n_state_half)), L12)),
+                torch.hstack((L21, torch.zeros((self.n_state_half, self.n_state_half)))),
             )
         )
-        A = np.real(IDFT @ sp.linalg.expm(L * self.sample_time) @ DFT)
+        A = torch.real(IDFT @ torch.linalg.matrix_exp(L * self.sample_time).to(dtype=IDFT.dtype) @ DFT)
         return A
 
     def _compute_control_sup(self):
@@ -281,7 +289,7 @@ class SchrodingerEnv(PDE):
             None.
 
         Returns:
-            control_sup (`ndarray` with shape `(n_state, n_action)): the control_sup matrix
+            control_sup (`Tensor` with shape `(n_state, n_action)): the control_sup matrix
             determines how each individual control input is distributed in the domain, where
             control_sup_width (float within (0, 1)) defines the width of the domain that each
             control input can affect. It is enforced that control_sup_width * n_actions <= 1.
@@ -289,7 +297,7 @@ class SchrodingerEnv(PDE):
             to the parent class, the control sup for Schrodinger equation only has nonzero values
             in the second half of the domain, see paper for more descriptions.
         """
-        control_sup = np.zeros((self.n_state, self.n_action))
+        control_sup = torch.zeros((self.n_state, self.n_action), device=self.device, dtype=self.dtype)
         domain_width = self.control_sup_width * self.n_state_half
         domain_spacing = int(self.n_state_half / self.n_action)
         for u_i in range(self.n_action):
diff --git a/controlgym/envs/utils.py b/controlgym/envs/utils.py
index 50b0fc5..659e242 100644
--- a/controlgym/envs/utils.py
+++ b/controlgym/envs/utils.py
@@ -1,3 +1,4 @@
+import torch
 import numpy as np
 from scipy.signal import cont2discrete
 
@@ -16,7 +17,7 @@ def ft_matrix(n_state):
     for i in range(n_state):
         for j in range(n_state):
             DFT[i, j] = omega ** (i * j)
-    return DFT
+    return torch.tensor(DFT)
 
 
 def ift_matrix(n_state):
@@ -29,9 +30,9 @@ def ift_matrix(n_state):
     Returns:
         IDFT: inverse discrete Fourier transform matrix
     """
-    DFT = ft_matrix(n_state)
+    DFT = np.array(ft_matrix(n_state))
     IDFT = np.conjugate(DFT.T) / n_state
-    return IDFT
+    return torch.tensor(IDFT)
 
 def c2d(A_cont, B1_cont, B2_cont, C1_cont, D11_cont, D12_cont, sample_time):
     """Discretize the continuous-time system.
diff --git a/controlgym/envs/wave.py b/controlgym/envs/wave.py
index f0b7347..e6421de 100644
--- a/controlgym/envs/wave.py
+++ b/controlgym/envs/wave.py
@@ -1,5 +1,6 @@
 import numpy as np
-import scipy as sp
+import math
+import torch
 from controlgym.envs import PDE
 from controlgym.envs.utils import ft_matrix, ift_matrix
 
@@ -69,6 +70,7 @@ class WaveEnv(PDE):
         observation_limit: float = None,
         reward_limit: float = 1e15,
         seed: int = None,
+        device: str = 'cpu',
     ):
         self.n_state_half = int(n_state / 2)
         PDE.__init__(
@@ -91,15 +93,17 @@ class WaveEnv(PDE):
             observation_limit=observation_limit,
             reward_limit=reward_limit,
             seed=seed,
+            device=device,
         )
 
         # set up the grid parameters: since the state x comprises both u in
         # the wave equation and its time derivative, the number of grid
         # points is half the dimension of x
-        self.domain_coordinates = np.linspace(
+        self.domain_coordinates = torch.linspace(
             0,
             self.domain_length - (self.domain_length / self.n_state_half),
             self.n_state_half,
+            device=self.device,
         )
 
         # set up the wave speed
@@ -120,20 +124,16 @@ class WaveEnv(PDE):
     def select_init_state(self, init_amplitude=None, init_spread=None):
         """Function to select the initial state of the PDE."""
         if init_amplitude is None:
-            random_amplitude = self.rng.uniform(
-                -0.5 * self.init_amplitude_width, 0.5 * self.init_amplitude_width
-            )
+            random_amplitude = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_amplitude_width
             init_amplitude = self.init_amplitude_mean + random_amplitude
         if init_spread is None:
-            random_spread = self.rng.uniform(
-                -0.5 * self.init_spread_width, 0.5 * self.init_spread_width
-            )
+            random_spread = (torch.rand(1, generator=self.rng, device=self.device) - 0.5) * self.init_spread_width
             init_spread = self.init_spread_mean + random_spread
-        init_u = init_amplitude * np.cosh(
+        init_u = init_amplitude * torch.cosh(
             1 / init_spread * (self.domain_coordinates - 1 * self.domain_length / 2)
         ) ** (-1)
-        init_v = np.zeros(self.n_state_half)
-        init_state = np.concatenate((init_u, init_v))
+        init_v = torch.zeros(self.n_state_half, device=self.device)
+        init_state = torch.cat((init_u, init_v))
         return init_state
 
     def step(self, action: np.ndarray[float]):
@@ -165,23 +165,30 @@ class WaveEnv(PDE):
         ), "Input control has wrong dimension, the correct dimension is: " + str(
             (self.n_action,)
         )
+
+        action_torch = torch.tensor(action, device=self.device, dtype=self.dtype)
+
         # generate the process noise, which is a Gaussian random vector with dimension n_state
-        disturbance = self.rng.multivariate_normal(
-            np.zeros(self.n_state),
-            self.process_noise_cov * np.identity(self.n_state),
+        disturbance = torch.normal(
+            mean=0.0,
+            std=self.process_noise_cov,
+            size=(self.n_state,),
+            generator=self.rng,
+            device=self.device,
+            dtype=self.dtype
         )
 
         # generate the observation
         observation = self._get_obs()
 
         # step the system dynamics forward for one discrete step
-        next_state = self.A @ self.state + self.B2 @ action + disturbance
+        next_state = self.A @ self.state + self.B2 @ action_torch + disturbance
 
         # compute the reward, which happens before updating the environment state
         # because the reward (might) depends on both the current state and the next state.
         # * In the default reward function, the dependence on the current state is
         # through the self.state attribute, which will not be updated until the next line.
-        reward = self.get_reward(action, observation, disturbance, next_state)
+        reward = self.get_reward(action_torch, observation, disturbance, next_state)
 
         # update the environment
         self.state = next_state
@@ -192,7 +199,8 @@ class WaveEnv(PDE):
         truncated = (
             False if self.reward_range[0] <= reward <= self.reward_range[1] else True
         )
-        info = {"state": self.state}
+        info = {"state": self.state.detach().cpu().numpy()}
+        observation = observation.detach().cpu().numpy()
 
         # return the observation and stage cost
         return observation, reward, terminated, truncated, info
@@ -204,54 +212,54 @@ class WaveEnv(PDE):
             None.
 
         Returns:
-            A (`ndarray` with shape `(n_state, n_state)).
+            A (`Tensor` with shape `(n_state, n_state)).
         """
-        DFT = np.vstack(
+        DFT = torch.vstack(
             (
-                np.hstack(
+                torch.hstack(
                     (
                         ft_matrix(self.n_state_half),
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                     )
                 ),
-                np.hstack(
+                torch.hstack(
                     (
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                         ft_matrix(self.n_state_half),
                     )
                 ),
             )
         )
-        IDFT = np.vstack(
+        IDFT = torch.vstack(
             (
-                np.hstack(
+                torch.hstack(
                     (
                         ift_matrix(self.n_state_half),
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                     )
                 ),
-                np.hstack(
+                torch.hstack(
                     (
-                        np.zeros((self.n_state_half, self.n_state_half)),
+                        torch.zeros((self.n_state_half, self.n_state_half)),
                         ift_matrix(self.n_state_half),
                     )
                 ),
             )
         )
         domain_wavenumber_complex = (
-            np.fft.fftfreq(self.n_state_half, self.domain_length / self.n_state_half)
+            torch.fft.fftfreq(self.n_state_half, self.domain_length / self.n_state_half)
             * 2
-            * np.pi
+            * torch.pi
         )  # wavenumbers
-        L12 = np.diag(np.ones(self.n_state_half))
-        L21 = np.diag(-self.wave_speed**2 * domain_wavenumber_complex**2)
-        L = np.vstack(
+        L12 = torch.diag(torch.ones(self.n_state_half))
+        L21 = torch.diag(-self.wave_speed**2 * domain_wavenumber_complex**2)
+        L = torch.vstack(
             (
-                np.hstack((np.zeros((self.n_state_half, self.n_state_half)), L12)),
-                np.hstack((L21, np.zeros((self.n_state_half, self.n_state_half)))),
+                torch.hstack((torch.zeros((self.n_state_half, self.n_state_half)), L12)),
+                torch.hstack((L21, torch.zeros((self.n_state_half, self.n_state_half)))),
             )
         )
-        A = np.real(IDFT @ sp.linalg.expm(L * self.sample_time) @ DFT)
+        A = torch.real(IDFT @ torch.linalg.matrix_exp(L * self.sample_time).to(dtype=IDFT.dtype) @ DFT)
         return A
 
     def _compute_control_sup(self):
@@ -262,7 +270,7 @@ class WaveEnv(PDE):
             None.
 
         Returns:
-            control_sup (`ndarray` with shape `(n_state, n_action)): the control_sup matrix
+            control_sup (`Tensor` with shape `(n_state, n_action)): the control_sup matrix
             determines how each individual control input is distributed in the domain, where
             control_sup_width (float within (0, 1)) defines the width of the domain that each
             control input can affect. It is enforced that control_sup_width * n_actions <= 1.
@@ -271,7 +279,7 @@ class WaveEnv(PDE):
             in the second half of the domain, see paper for more descriptions.
         """
         # generate the control support matrix
-        control_sup = np.zeros((self.n_state, self.n_action))
+        control_sup = torch.zeros((self.n_state, self.n_action), device=self.device, dtype=self.dtype)
         domain_width = self.control_sup_width * self.n_state_half
         domain_spacing = int(self.n_state_half / self.n_action)
         for u_i in range(self.n_action):
-- 
2.40.1.windows.1

