[project]
name = "domain"
description = "Setting Up Domain for VintixII model"
requires-python =">=3.8"
dependencies = []
authors = [
    {name = "polubarov", email = "polubarov@airi.net"}
]
dynamic = ["version"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["domain"]